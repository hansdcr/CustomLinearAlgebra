# Git 快速命令参考卡

## 🌿 分支结构

```
main        → 稳定版本(每个迭代完成后合并)
├── ai-dev  → Claude的工作分支
└── learning → 您的学习和练习分支
```

---

## 📍 查看当前分支

```bash
git branch --show-current
```

---

## 🔄 切换分支

### 切换到AI工作分支
```bash
git checkout ai-dev
```

### 切换到您的学习分支
```bash
git checkout learning
```

### 切换到主分支
```bash
git checkout main
```

---

## 📥 同步AI的新代码到学习分支

### 方法1: 合并(保留您的更改)
```bash
git checkout learning
git merge ai-dev
# 如果有冲突,解决后:
# git add .
# git commit -m "合并ai-dev的新代码"
```

### 方法2: 完全同步(丢弃您的更改)
```bash
# 先备份
git checkout learning
git branch learning-backup-$(date +%Y%m%d)

# 完全同步
git reset --hard ai-dev
git push -f origin learning  # 如果需要推送
```

### 方法3: 只同步特定提交
```bash
# 查看ai-dev的提交
git log ai-dev --oneline -5

# 选择性合并(假设commit是abc123)
git checkout learning
git cherry-pick abc123
```

---

## 📊 查看差异

### 查看两个分支的差异
```bash
# 查看ai-dev比learning多了什么
git diff learning..ai-dev

# 只看文件列表
git diff --name-only learning..ai-dev
```

### 查看特定文件的差异
```bash
git diff learning..ai-dev -- src/core/vector2d.py
```

---

## 📝 提交代码

### 查看当前状态
```bash
git status
```

### 提交更改
```bash
# 添加所有更改
git add .

# 或添加特定文件
git add src/core/vector2d.py

# 提交
git commit -m "练习: 实现向量加法"

# 推送到远程(如果需要)
git push origin learning
```

---

## 🔍 查看历史

### 查看提交历史
```bash
# 当前分支
git log --oneline -10

# 所有分支(图形化)
git log --all --graph --oneline

# 查看ai-dev的历史
git log ai-dev --oneline -5
```

---

## 🏷️ 标签操作

### 查看所有标签
```bash
git tag -l
```

### 回到某个标签
```bash
git checkout iter01-complete
```

---

## 🆘 撤销操作

### 撤销未提交的更改
```bash
# 撤销单个文件
git restore src/core/vector2d.py

# 撤销所有更改
git restore .
```

### 撤销已提交但未推送的提交
```bash
# 回退一个提交(保留更改)
git reset HEAD~1

# 回退一个提交(丢弃更改)
git reset --hard HEAD~1
```

### 暂存当前更改
```bash
# 暂存(临时保存)
git stash

# 恢复暂存
git stash pop

# 查看暂存列表
git stash list
```

---

## 🎯 典型工作流

### Claude的日常工作流
```bash
# 1. 切换到ai-dev
git checkout ai-dev

# 2. 确保最新
git pull origin ai-dev

# 3. 开发代码...

# 4. 提交
git add .
git commit -m "迭代X: 实现XXX"
git push origin ai-dev
```

### 您的日常工作流
```bash
# 1. 切换到learning
git checkout learning

# 2. (可选)同步AI的新代码
git fetch origin
git merge origin/ai-dev

# 3. 练习和实验...

# 4. (可选)提交练习成果
git add .
git commit -m "练习: XXX"
```

---

## 🚀 一键命令

### 查看三个分支的状态
```bash
echo "=== Main ===" && git log main --oneline -3 && \
echo -e "\n=== AI-Dev ===" && git log ai-dev --oneline -3 && \
echo -e "\n=== Learning ===" && git log learning --oneline -3
```

### 同步所有分支
```bash
git fetch --all
```

### 清理已合并的分支
```bash
git branch --merged main | grep -v "main\|ai-dev\|learning" | xargs git branch -d
```

---

## 💡 提示

- **频繁提交**: 小步提交,方便回滚
- **清晰的提交信息**: 使用描述性的commit message
- **定期同步**: 每天开始前 `git fetch` 查看新代码
- **不怕实验**: 在learning分支随意实验,随时可以重置
- **备份重要实验**: `git branch experiment-name`

---

## 🔗 快速链接

- 完整文档: `docs/Git工作流程.md`
- 迭代总结: `docs/迭代X-总结.md`
- 学习计划: `docs/迭代计划详解.md`

---

**记住**: 在 `learning` 分支,您可以自由实验,不用担心破坏任何东西! 🎨
